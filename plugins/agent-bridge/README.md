# agent-bridge

**Role:** compatibility

Thin compatibility surface over the core send path, so the existing CLI keeps working identically during migration. Parity is proven before cutover.

> Status: scaffold. See ../../ARCHITECTURE.md and CONTRIBUTING.md.

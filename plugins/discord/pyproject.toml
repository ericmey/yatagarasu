[project]
name = "yatagarasu-discord"
version = "0.0.0"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["yatagarasu_discord*"]
